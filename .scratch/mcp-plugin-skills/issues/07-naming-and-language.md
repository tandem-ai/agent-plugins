# Skill names, namespace and language
Type: grilling
Status: open

## Question

Draft: plugin `tandem`, skills `start`, `sync`, `setup` (so `/tandem:start` in Claude Code, `$start` in Codex, `@start` in ChatGPT), written in English. Are the names right for a non-technical FDE (`start` vs `work`, `sync` vs `save`), do we want a French variant of the skills for French-speaking customers, and does `setup` deserve model invocation (today user-invoked only)?
