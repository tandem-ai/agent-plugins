# Validate every manifest against a real client
Type: task
Status: open
Blocked by: 01

## Question

Before the first tag: `claude plugin validate .` and an install from this repository in Claude Code and claude.ai (chat and Cowork); `codex plugin marketplace add` from this repository; Cursor `/add-plugin`; `gemini extensions install`; `agy plugin install`. Record what each client accepted or rejected, fix the manifests, and note the tool name prefix each client gives the Tandem MCP tools (needed if skills ever declare `allowed-tools`).
