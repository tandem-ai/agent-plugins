# Cursor deep link with a remote URL config
Type: research
Status: open

## Question

README ships `cursor://anysphere.cursor-deeplink/mcp/install?name=tandem&config=<base64 {"url":…}>`. Cursor's docs only show a stdio example. Confirm on a real Cursor install that a remote `url` config installs and triggers OAuth; otherwise replace the link with the mcp.json snippet.
