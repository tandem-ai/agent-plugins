{
  "name": "@tandem-ai/agent-plugins",
  "version": "0.2.0",
  "private": true,
  "description": "Tandem plugin for AI assistants: MCP connector + skills, one repo, one manifest per platform.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/build.mjs --check"
  },
  "license": "MIT"
}
